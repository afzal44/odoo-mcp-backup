{
  "_comment": "Drop this into your MCP client. Claude Desktop: ~/.config/Claude/claude_desktop_config.json (or the app's config dir). Claude Code: `claude mcp add`. The server talks stdio; the venv python is required so the mcp package is on the path.",
  "mcpServers": {
    "odoo-functional": {
      "command": "/root/.openclaw/workspace-odoo-ops/odoo_agent/.venv/bin/python",
      "args": ["/root/.openclaw/workspace-odoo-ops/odoo_agent/mcp_server.py"]
    }
  }
}
